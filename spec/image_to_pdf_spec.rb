require 'spec_helper'

describe ImageToPdf do
  it 'has a version number' do
    expect(ImageToPdf::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
